# Verification Before Completion

Probar y verificar todo ANTES de decir "completado". Nunca afirmar éxito sin evidencia real.

## Token Economy
- Verificar con un solo comando (no múltiples intentos fallidos)
- Si la verificación falla, reportar error exacto inmediatamente
- Nunca hacer `echo "done"` sin ejecutar realmente
